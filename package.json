{
  "name": "mesa-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ cryptonative-ch/mesa-subgraph",
    "create-local": "graph create --node http://localhost:8020/ cryptonative-ch/mesa-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ cryptonative-ch/mesa-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cryptonative-ch/mesa-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0"
  }
}
